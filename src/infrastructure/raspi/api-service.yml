apiVersion: v1
kind: Service
metadata:
  name: menu-maker-api-service
  namespace: menu-maker-dev
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: menu-maker-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 9376